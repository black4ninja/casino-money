{
  "name": "casino-money-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace root — orquesta los scripts de frontend/ y backend/ con yarn.",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND ,FRONTEND\" --prefix-colors \"bgCyan.black.bold,bgMagenta.white.bold\" --prefix \" {time} {name} \" --timestamp-format \"HH:mm:ss\" \"yarn --cwd backend dev\" \"yarn --cwd frontend dev\"",
    "dev:backend": "yarn --cwd backend dev",
    "dev:frontend": "yarn --cwd frontend dev",
    "typecheck": "yarn --cwd backend typecheck && yarn --cwd frontend typecheck",
    "test": "yarn --cwd frontend test",
    "build": "yarn --cwd backend build && yarn --cwd frontend build",
    "install:all": "yarn install && yarn --cwd backend install && yarn --cwd frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
